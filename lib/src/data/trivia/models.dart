/// Used to obtain a quiz by specified difficulty.
///
/// ! Don't rename fields !
enum TriviaQuizDifficulty { any, easy, medium, hard }

/// Used to get a quiz that has 4 answers
/// or yes/no answer
/// or any of the following
///
/// ! Don't rename fields !
enum TriviaQuizType { any, multiple, boolean }
